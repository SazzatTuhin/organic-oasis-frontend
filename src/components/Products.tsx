import SectionTitle from "./SectionTitle";

const Products = () => {
  return (
    <section className="wrapper">
      <SectionTitle title="Browse All products"></SectionTitle>
    </section>
  );
};

export default Products;
